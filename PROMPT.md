# Wavelength — AI Build Prompt

Use this file to guide your AI assistant when building Wavelength features.

## Technology Stack

- React 18 with TypeScript
- Vite for bundling and dev server
- React Router v6 for routing (routes defined in `App.tsx`)

## Project Structure

```
src/
├── api/client.ts       — API fetch functions (already implemented)
├── types/index.ts      — TypeScript type definitions (already defined)
├── App.tsx             — Route definitions (already configured)
├── App.css             — Base CSS reset
├── pages/              — Page components (create these)
├── components/         — Shared components (create these)
```

## Instructions

Build the pages and components for the Wavelength podcast app as described in `SPEC.md`.

Use the API client functions from `src/api/client.ts` and types from `src/types/index.ts`.
